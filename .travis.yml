language: node_js
node_js:
  - node
matrix:
  include:
    - os: osx
      addons:
        chrome: stable
        firefox: latest
        sudo: required
    - os: windows
      env: WINDOWS=true
      before_install:
        - choco install chromium
        - choco install firefox
script:
  - npm start
cache: npm